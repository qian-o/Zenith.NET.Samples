<Page x:Class="Sample.WinUI.Views.MainView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:sample="using:Sample"
      xmlns:zenith="using:Zenith.NET.Views.WinUI">

    <Grid>
        <zenith:ZenithView GraphicsContext="{x:Bind sample:Renderer.Context}"
                           RenderRequested="{x:Bind ViewModel.OnRenderRequested}" />

        <ComboBox Margin="10"
                  HorizontalAlignment="Right"
                  VerticalAlignment="Top"
                  ItemsSource="{x:Bind sample:Renderer.Samples}"
                  SelectedItem="{x:Bind ViewModel.Sample}" />
    </Grid>

</Page>
