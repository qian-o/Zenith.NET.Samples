<Page x:Class="Sample.WPF.Views.MainView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:sample="clr-namespace:Sample;assembly=Sample"
      xmlns:viewmodels="clr-namespace:Sample.WPF.ViewModels"
      xmlns:zenith="clr-namespace:Zenith.NET.Views.WPF;assembly=Zenith.NET.Views.WPF"
      Title="MainView"
      d:DesignHeight="450"
      d:DesignWidth="800"
      mc:Ignorable="d">

    <Page.DataContext>
        <viewmodels:MainViewModel />
    </Page.DataContext>

    <Grid>
        <zenith:ZenithView GraphicsContext="{x:Static sample:Renderer.Context}">
            <b:Interaction.Triggers>
                <b:EventTrigger EventName="RenderRequested">
                    <b:InvokeCommandAction Command="{Binding RenderCommand}"
                                           PassEventArgsToCommand="True" />
                </b:EventTrigger>
            </b:Interaction.Triggers>
        </zenith:ZenithView>

        <ComboBox Margin="10"
                  HorizontalAlignment="Right"
                  VerticalAlignment="Top"
                  ItemsSource="{x:Static sample:Renderer.Samples}"
                  SelectedItem="{Binding Sample}" />
    </Grid>

</Page>
